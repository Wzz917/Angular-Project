export class Task {
    id: number;
    name: string;
    times: number;

    constructor(id:number, name:string, times: number) {
        this.id = id;
        this.name = name;
        this.times = times;
    }
    
}
